/*
* 用户行为分析数据
*/
const state = {
	trajectory : []
}

// getters
const getters = {}

// actions
const actions = {
  	submit () {
    	
  	}
}
// mutations
const mutations = {
  add () {
    
  }
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}